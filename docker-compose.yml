services:
  postgres:
    image: postgres:17.0
    container_name: postgres-db-senlanotificationmicroservice
    environment:
      POSTGRES_DB: notificationmicroservice
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d senlatestproject" ]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: